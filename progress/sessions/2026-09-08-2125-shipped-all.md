# S161 · 2026-09-08 18:50–21:25 · 用户授权「直接都干完」：sm-toolkit 合 main + launchd 常驻；Trellis 四波上线 + 单项目切流；冷启动 P1 在修

## 落地清单

| 项 | 结果 |
|---|---|
| 焚决 fj runner → main | 477cdef，bundle sha 与安装副本一致 |
| ingress 集成（s3 + s4 + 只读白名单）| f25aac0：17/12 升级检查与冒烟全绿，schema 差异空 |
| sm-toolkit feat/codex-ingress → main | PR #16 合并 40ddc5b；主仓构建 dist 含 ingress |
| daemon 常驻 | launchd `com.smokingmouse.agent-server`（KeepAlive/RunAtLoad，PATH 含 bun/node/codex），pid 71466，`codexIngressUrl ws://127.0.0.1:55919`；切换前库备份 + integrity_check |
| Trellis 四波集成 feat/ship-d | 桥 → 影子 → 第二步 → mobile wave3/wave1，安全区脚本失败根因是集成后 CSS 重复定义 env(safe-area-inset-*)（3b3449f 修） |
| Trellis 上线 | PR #44 合并 d19fd63，release 20260908T131453 上线，备份 `~/.trellis/backups/20260908T131515.db`，前版本 92c6539 |
| 切流 | `TRELLIS_AS=on` + `TRELLIS_AS_PROJECT=on` + 新增 `TRELLIS_AS_PROJECT_ID=f019e35e-…`（单项目），真实 sonnet 会话 + 手机审批全链路通过；回退：`TRELLIS_AS=off` 重启或删 `TRELLIS_AS_PROJECT`，版本回退 `make rollback` |
| vendor | Trellis vendor/agent-server 同步到 sm-toolkit main 40ddc5b（新 vendor 拒绝隐式 model，fixtures 改显式 sonnet） |

## 实弹与坑

- 三次卡在「分支没推远端、坐席按约不 push」：feat/codex-ingress、feat/ship-d 由 leader 推送后坐席继续；下次上线单的授权卡应显式给 push 权限。
- 验收命令两处误红：git 对非 ASCII 路径加引号（焚决）；桥合并后手机脚本需隔离前缀（facts.md）。
- 生产冒烟抓到 P1：官方 TUI 冷启动 thread/start 带本机 config.toml 的 config 键，ingress router.ts:103 只认小名单 → `-32005 native config overrides are not supported`；fj 的「AS 建线程 + resume」路径不受影响。修复单 ingress-fresh-start 在跑（按键分流 + fresh_tui_session_ok + `--mode prod` 冒烟），PR 合 main 后由 leader 重启 launchd。
- 3490 验证实例已停；内存清理见 S159/S160。

## Next

- fresh-start 修复 → 合 main → 重建 → launchd kickstart → 生产冒烟（两后端冷启动）→ 通知用户可直接 `codex --remote` 用。
- 切流观察一天后扩到全部项目（去掉 PROJECT_ID）；agent-tui 一周后删；backlog P2 清扫。
