# S167 · 2026-09-09 04:40–11:50 · 波 4 地图被用户否决→回滚→返工布局→重新上线

## 事件

- 用户看了一个 6 节点会话的地图：「这个地图太乱了，我感觉还不如以前」。核对截图：坐席把话题块内节点排成 4 列网格，父子关系读不出来；连线在格子间乱走、绕过标题栏、互相交叉；fitView 只做「缩到放得下」没有「放大到能读」，6 个节点缩成一小坨悬在整屏空白中间。坐席验收只盯 124 节点极端案例（124/124 可见），小会话没人看——**主控验收也只看了 124 节点那张图**，是我的失误。
- `NEXT_PUBLIC_TRELLIS_CANVAS_MAP` 是构建期 flag，运行时改不了；立即 `make rollback` 回波 3 release（3a26f47d8），旧画布回来，其它三波不受影响。
- 返工单 fj-canvas-map-layout-3a3c（codex，fix/canvas-map-layout）：块内真树层级布局（父上子下、只画父→子边、正交/平滑、几何检查零交叉零穿卡）、话题块流式换行、小会话放大到标题可读并居中、大会话保留色块退化；`lib/canvas-map.test.ts` 8 项；同会话同视口旧画布/新地图对照 8 张（6 节点与 124 节点 × 桌面/手机）。主控核对：6 节点是一条清晰的 #1→#6 竖链、标题三行可读；124 节点每个话题都是可辨层级的树、全部入屏。PR 合 main 并重新部署（见本条 Next）。

## 教训（写进契约模板）

- 界面类交付的验收截图必须覆盖**小、中、大**三档真实数据，不能只拿极端案例；主控自己也要看小案例。
- 构建期 flag 不是回滚手段；回滚靠 `make rollback`，flag 只用于长期共存。

## Next

- 确认地图重新上线的 release，用户复看 6 节点会话。
- 待用户：`FJ_RUNNER_DEFAULT=codex-tui`；待办层；非 git 项目归属。
- 观察一天后 AS 切流扩到全部项目；backlog P1/P2 如 S166。
