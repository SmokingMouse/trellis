### Session 139（2026-09-05，leader 编队：手机端体验优化第一波 + prod 配 TRELLIS_PUBLIC_URL）
- **触发**: 用户「你作为 leader」接管后提出「优化手机上的 Trellis 体验，各种操作都特别不便利」。追问得到三条输入：Safari 加到主屏（standalone）使用；手机只做四件事——看回答、追问、打审批卡、开新会话；最难受的是桌面版大量复杂功能配置堆在主页上盖住核心体验。方向由此定为「手机精简壳：首屏只露四件事，桌面功能不删只藏，桌面端零回归」。
- **开场核对**: prod = main = 22f6044，网关 gate up / auth on，飞书 bot 在线。真库近 7 天：节点 +6、会话 +3、定时任务 0 次运行（最后一次 8 月 4 日）、绑飞书落点的任务 0 个——本周上线的两件事尚未被用起来。8 月 28 日遗留无契约任务目录 `fj-mt-image-681e` 移至 /tmp/fenjue-orphans（其 out/ 两个脚本原为 git 跟踪文件，工作树显示为删除，待随 progress 一并提交或还原）。
- **审计单**（`fj-audit-f928`，codex gpt-5.6-sol，只读，约 20 分钟）: 用真库在线备份起隔离实例（`bun --bun run start -- -p 3467`，非 next start；memory 里旧起法应订正），agent-browser iPhone 15 视口走完十条动线，23 张截图。结论 P0 0 / P1 10 / P2 2：M1 顶栏 393px 视口内 scrollWidth 763 无横滚入口；M2 TreePanel 默认展开悬浮盖住回答与 Composer；M3 无 viewport-fit=cover、全仓无 safe-area，键盘态 Composer 底边距屏底 12px；M4–M9 追问/审批/新会话/复制/分支/新树控件热区 20–40px；M10 设置页 tabs 溢出；M11 画布 280px 卡片多数在视口外；M12 缺 theme-color meta、无 standalone 分支。§4 给出留/藏裁决表与 5 项拆解。审计产物在 `.fenjue/archive/fj-audit-f928/out/`。
- **计划**（`.fenjue/plan.md`）: 撤掉原拟「方案」项——审计已细到 file:line 与验收方式，再写方案只是复述。两波：一波 `mobile-safe-area` / `mobile-touch-targets` / `mobile-slim-shell` 并行 worktree → `review-a`（opus 异源）→ `ship-a`（问用户）；二波 `mobile-followup-approval` / `mobile-new-session`（after ship-a）→ `review-b` → `ship-b`。每单 verify = `bunx tsc --noEmit` + `bun test` + 自包含 `scripts/mobile-verify/<slug>.sh`（隔离实例 + agent-browser iPhone/键盘态/桌面对照）+ 端口回收。
- **一波进展**: `feat/mobile-safe-area`（805bc5d…，viewport-fit、safe-area 变量四类消费者、theme-color、dvh 清残留）与 `feat/mobile-touch-targets`（7b330f9 + d4f2191，Button/IconButton 手机 44px 变体、审批/Ask/drawer/复制/分支/新树逐点覆盖、库副本等待态 fixture）均验收通过、未合并。`feat/mobile-slim-shell` 18:10 起单。
- **prod 配置**（`fj-public-url-d9c7`）: `TRELLIS_PUBLIC_URL=https://trellis.home.smokingmouse.cn` 写入 `~/Library/LaunchAgents/com.smokingmouse.trellis.plist`（备份 `~/.trellis/backups/com.smokingmouse.trellis.plist.20260905T173750`），`launchctl bootout` + `bootstrap` 重读（**kickstart -k 不重读 EnvironmentVariables**），新进程 pid 38087，gate up，飞书 bot 17:38 重连。plist 不由 make deploy 生成（mtime 8/30 早于 9/3 部署），直接改可持久。域名解析为 fake-ip 198.18.0.237（本机代理），https 200 且命中 3088 的 gate。
- **编队故障与处置**: ① 结算把主控未提交的 progress 文件（README + S137/S138）判成坐席越界——基线未记脏文件；两次用 `git stash push -u -- progress/` → `fj settle --force` → close → `stash pop` 放行，根治需提交 progress（等用户）。② codex 起位时自动升级 0.153.2→0.153.4 后退出，`launch` 就绪门与契约注入均误报成功；原 pane 重起 + `contract render` 手动注入 + 重置 opened_at 后 6 分钟交付。③ 验收脚本在结算侧三种失败：依赖 `rg`；主控沙箱 Bash 内 agent-browser 守护进程 EAGAIN；脚本无登录步骤、靠残留 cookie 才能过。处置：结算改无沙箱运行；后续契约写死「全新 profile + env -i + stdin /dev/null 连跑两遍」的交付判据。
- **Next**: ① slim-shell 交付后起 review-a（opus 官方，异源），pass 后问用户合并上线（ship-a）；② ship-a 后二波两单并行；③ 用户拍板：留/藏表微调、提交 progress（README + S137/S138/S139 + 编队台账 + 被移走的两个旧脚本）；④ 清理：`feat/task-lark-landing` `feat/recent-chain-status` 本地+远端已合并分支，一波 worktree 合并后移除；⑤ 回填 herdr-leader seats.md：codex 自动升级绕过 launch 就绪门、验收脚本三类环境陷阱。
