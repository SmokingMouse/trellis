# S177 · 2026-09-18 01:00 → · leader：BOE devbox prod 卡顿两处 watcher 缺陷

## 做了什么
- 收到 devbox 运维故障报告并逐条核实代码：A `herdr-fleet.ts` attachTranscript 的 catch 里 delete key → 去重失效 → 每次 snapshot/pane 事件重试（24h 46 次），触发数据是合法空会话（`seedLineage` 对 0 turn 抛错）。B `cli-import-db.ts` 每次 reimport 全量 `parseCliTranscript`，无 mtime/size 短路与偏移；185MB codex rollout 持续写入 + 600ms 去抖 → 事件循环阻塞。
- 定重档：fix-a / fix-b 两单并行（opus），异源 review（codex）后合 main。devbox 已临时 `TRELLIS_HERDR=off` 止血，A 的错误归零、B 的尖峰仍在。

## 决定
- [decision] 两单并行不串行：改的是不同关注点，只有 cli-sync-watcher.ts 远端两处区域相交，靠 worktree 隔离 + 契约里写死各自禁区。

## Next
两单交付 → review → 合 main；本机 prod 是否同步部署问用户。磁盘 94% 与写失败静默转圈另议。
