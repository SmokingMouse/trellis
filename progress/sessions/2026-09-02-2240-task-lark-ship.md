### Session 136（2026-09-02 晚，接 S135：用户拍板「合并」→ PR #38 合并 + prod 上线 8ff5084）
- **触发**: 用户一个字「合并」。按编队规则派 codex（gpt-5.6-sol）跑只读契约 + 逐条写死的副作用：推分支 → `gh pr create` → `gh pr merge --merge`（保留远端分支）→ 主仓 `git pull --ff-only`（不碰主控未提交的 progress 文件）→ `make deploy` → 验活。硬闸：自动回滚一次即停报 failed、不重试；不建不跑 prod 任务。
- **过程**: PR #38 `feat(tasks): 定时任务落进飞书会话——…` merge commit 8ff5084；`make deploy HEAD` → release `20260902T143231-8ff508439`，preflight / smoke 四项 + 认证闸 / 库快照 `backups/20260902T143244.db` / 切换 / 验证全过，未触发回滚；旧 release 69c7b3eae 被 gc，previous = 526d3abb0。飞书 bot 部署后约 2 分钟重连，`svc-trellis.log` 见「长连接就绪」。codex 沙箱框 8 次（push / pr create / pr merge / pr view / pull / deploy / 两次验活脚本）逐个读屏放行，全部与契约命令一致。
- **验收**: 主控 settle 独立复跑 6 条全绿——PR merged；主仓 main = origin/main = 8ff5084 且含 4c518ec；`~/.trellis/current` 指向 8ff508439；`/__gate/health` gate up / next ready / auth on，`lark_bots.last_error` 空且最近连接 <30 分钟；prod 真库 `tasks` 已有 `lark_bot_id/lark_chat_id`（加法迁移在启动时落地）；worktree 零改动。
- **产物**: `.fenjue/archive/fj-task-lark-ship-610e/`（deploy.log + settlement）；台账 +1。坐席已退位，worktree `~/worktrees/trellis/task-lark-landing`（workspace w1C，仅剩锚点 pane）与远端分支 `feat/task-lark-landing` 均保留，待用户确认后清理。
- **Next**:
  1. **真群验收（用户触发，会真发飞书消息）**：任务页把「仓库日报（试跑）」绑 agent + 落点群「消息推送」→ 手动跑一次 → 群里出现推送 → 话题内不 @ 追问接叶子 → 引用推送提问在该节点下分支。这也是路线 A 的第一个 dogfood 用例。
  2. **配置 `TRELLIS_PUBLIC_URL`**（用户给平时打开 Trellis 的地址）：写进 `com.smokingmouse.trellis.plist` 的环境变量后 kickstart；未配前推送只有文案没链接。
  3. 小修候选：S135 记的 N1（回退告警节流 + 尊重 notify_on）、N2（落点提示只在已选时渲染）；trellisctl 与 `skills/trellis-admin` 学 `larkBotId/larkChatId`。
  4. 清理（待确认）：`herdr worktree remove --workspace w1C`、远端分支、`/tmp/fj-tll-*` `/tmp/rev-*.db` `/tmp/re-*.ts` `/tmp/poc-*.ts` 草稿。
  5. 本 session 与 S135 的 progress 改动（README Focus、facts +1、sessions 两条、台账 +4 行）未提交，等用户说提交。
