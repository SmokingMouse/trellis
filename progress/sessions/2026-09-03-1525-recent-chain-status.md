### Session 137（2026-09-03，leader 编队：侧栏「最近」分组状态灯以链为最小粒度；分支 `feat/recent-chain-status` 双审通过待合并）
- **触发**: 用户「左侧最近聊天，运行状态指示灯需要以链作为最小粒度」。只读查证（Sonnet Explore）结论：链行其实已按 `chain.status` 画灯，缺的是**粒度与实时**——服务端 `listRecentChains` 只看链尾三字段；`/api/runs` 把 run-bus 的 nodeId 级活跃集合坍缩成 `Set<sessionId>`；`/api/recent` 只在会话级 `runningKey` 变化时重拉。三种失灵场景：同会话链 A 跑完链 B 接着跑、当前会话本地起 run（会话行靠 `activeRunning` 立刻亮、链行滞后）、在跑节点不是链尾。
- **编队**: 实现 codex gpt-5.6-sol（cpa），worktree `~/worktrees/trellis/recent-chain-status`（基于 fd33615）。首版 118ffe4（11 文件 +410/-125，20 分钟）settle 6 条全绿（tsc / bun test 52→56 / build / 隔离实例活体 / spec / 提交）。异源 review（Claude 官方 opus）**verdict fail**：F1 major「最近」会话行把 session 级 running 位图整个换成链聚合，1.5 秒轮询窗口与 `/api/recent` 拉取失败时会话行比改前更暗；F2 major 整链 `has_error` 沿 lineage `max()` 传染，真库 11/184 条链尾已读完的链永久标红且无入口清除（`chain_error` 21 vs `tip_error` 7）；F3 minor 整链 unread 同类（4 条清不掉）；F4 minor 无活跃 run 时也按 error/unread 重排「最近」；F5 minor 删可见性门控时把 `visibilitychange` 立即重拉也删了。核验通过 14 项（run-bus 生命周期无改动无泄漏、`/api/runs` 四消费方兼容、`/api/recent` 不每轮重拉、真库 CTE 3ms、载荷最坏约 28KB）。
- **主控裁决**: 生成中 / 等输入按**整条链**判（实时集合）；出错 / 未读回到**链尾**语义（DB 基线，含链尾 `status='streaming'` 仍算 streaming，保证新节点刚出现时链行也亮）；「最近」会话行 = 链聚合与 session 位图取较高优先级，永远不比改前更暗；提升只按实时二值 waiting=2 > streaming=1 > 0，error/unread 不影响顺序；`useRunPolling` 恢复基线门控（隐藏不轮询 + 回前台立即拉一次）。返工 d970da9（6 文件 +149/-45，12 分钟），settle 全绿（单测 61）；同一 opus 复审增量 **pass**：真库 `tip_error` 21→7、`tip_read_but_chain_unread` 恒 0、F4 反例消失、轮询钩子与基线逐行只剩载荷 hunk、24 条兜底 PoC 断言全过。
- **最终形态**（spec `progress/recent-chains.md`，在分支上）: `/api/runs` 保留 `runningSessionIds`，新增带 sessionId 的 `runningNodes` / `waitingNodes`；store 新增 `runningNodeIds` / `waitingNodeIds`；`RecentChain` 带整条 lineage 的 `nodeIds`，`ChainRow` 与实时集合求交现算；节点级 key 变化重拉 `/api/recent`；服务端截断前与客户端露出前各按实时态提升一次，会话亮时下面必有一条可见链行同样亮。活体：本地起 run 后 1.8 秒链行与会话行同时亮；中段节点重跑同时点亮两条包含它的链。
- **遗留（nit）**: N1 两条「中段 error/unread 不污染」单测在纯函数层恒真，真正的修复在 CTE 无测试覆盖（下次改回 `max()` 不会红）；N2 会话兜底把 running 一律落成 streaming，唯一 run 停在等输入且链未下发时会话行显示「生成中」而非「等你回答」（与基线相同，非回归）。
- **产物**: 分支 `feat/recent-chain-status` 两个 commit（118ffe4 + d970da9，未 push）；review / 复审报告与结算在 `.fenjue/archive/fj-recent-chain-status-6b56` `fj-rcs-review-0163` `fj-rcs-rereview-7dd1`；台账 +3。坐席已退位，worktree（workspace w1F，仅剩锚点）保留。
- **Next**: ① 用户拍板合并 → 同 S136 流程（push / PR / merge / `make deploy`）；② 可选小修 N1（给 CTE 加一条 `bun --conditions react-server` 的 repo 级测试）与 N2；③ 上一单遗留仍在：真群验收、`TRELLIS_PUBLIC_URL`、S135 的 N1/N2、trellisctl 学字段、两个 worktree（w1C / w1F）与远端分支的清理；④ 本条与 README Focus 未提交。
