### Session 143（2026-09-06 12:00–13:30，接 S142：卡片级「稍后再读」+ 手机阅读时顶部随滑隐藏，集成分支 feat/mobile-wave3 复核中）
- **触发**: 用户 ①「能增加一个卡片粒度的收藏功能吗？学习类项目临时没时间读，弄成稍后再读」；②（看到输入区下滑隐藏后）「顶上那段区域也能隐藏吗？阅读时不受其它区域干扰」。
- **决策（主控定，写进 spec）**: 稍后再读——D1 入口 = 侧栏「稍后再读 (N)」分组（在「最近」之上）+ 手机 overflow 项 → bottom sheet；D2 打开不自动移除，只有卡片再点或列表「读完 ✓」移除，收藏与已读正交；D3 粒度 = 节点。顶部隐藏——随下滑隐藏 slim header + 标题条，上滑/到顶/点顶边恢复，等待横幅不隐藏，桌面不变。
- **实现（并行两单）**: `feat/read-later`（reuse codex `trellis-wave1-fix`，18 文件 +928：`nodes.bookmarked_at` 幂等迁移、repo `setNodeBookmark/listBookmarks`、`PATCH /api/nodes/:id {bookmarked}` + `GET /api/bookmarks`、`BookmarkButton` 桌面动作区 + 手机 … 菜单、`BookmarkRows` 侧栏分组、`BookmarksDrawer` 手机 sheet、`stores/sessionStore` bookmarks/refresh/toggle、spec `progress/read-later.md`、脚本 `mobile-read-later.sh` 端口 3476、单测 `lib/server/bookmarks.test.ts`）；`feat/mobile-header-hide`（新 codex 坐席，4 文件 +198：`hooks/useScrollHide.ts` 共享滚动隐藏态、Header/标题条 translateY + inert/aria-hidden、`[data-header-reveal]` 顶边恢复条、横幅贴 safe-top、断言进 `mobile-slim-shell.sh`）。均一次过 settle。
- **编队摩擦**: ① 首次 launch 因基线 verify 跑 4 条浏览器脚本超过工具 5 分钟上限被掐，留下陈旧 `owner.lock`；`task launch --steal` 不存在（fj 崩 Unknown option），手动删 lock 后后台重起成功——起位一律后台跑；② 两坐席并行时 `agent-browser close --all` 会清掉对方会话（read-later 的 3476 会话被 slim-shell 清成 about:blank）——改为只 close 自己的命名 session，写进后续契约。
- **review-c**（opus reuse，pass）: 无 major；R-1 `listBookmarks` 未过滤雪藏树（hidden_at 只在树根）、R-2 `refreshBookmarks` 把 top-50 之外的收藏本地清成未收藏、R-3 摘要无省略号、R-4 未读点/sheet 焦点 a11y、H-1 隐藏靠 transform 位移 104px 阅读锚点上跳（违背契约「锚点不跳」）、H-2 非 standalone 下顶边恢复条仅 8px、H-3 `useScrollHide` 模块级单例；三方合并零冲突；reviewer 在 /tmp worktree 无法构建（Turbopack `Invalid distDirRoot`），合并态脚本共存未实测。
- **fix-c**（reuse fixer，集成分支 `feat/mobile-wave3`，22 文件 +1402）: 合并两支 + 修 R-1/R-2/R-3/R-4/H-1/H-2（雪藏树解析到根过滤 + 单测；只同步窗口内 id + API 返回 total；「…」；role/焦点；同帧 scrollTop 补偿 + 锚点位移 ≤2px 断言；恢复条下限 24px + 标题条区域可点），七条脚本两轮全绿、tsc、68 单测。→ `review-c2` 进行中。
- **状态**: `feat/mobile-wave3` @ f5ab232 待复核；`ship-c` 契约已建（`make deploy REF=HEAD`、blocker 期间不发 result、commands_forbidden 已收窄），等复核 pass + 用户放行。手机一二波已上线（S142），用户尚未反馈真机验收。
- **Next**: ① review-c2 pass → 问用户上线 → 起 ship-c（settle 前更新 base_commit、stash 主控 progress 文件）；② 上线后用户真机验收：收藏一张卡 → 侧栏/overflow 计数 → 列表跳转 → 读完；阅读下滑顶栏隐藏；③ 清理待用户确认（worktree 现共 10 个、本地 feat/* 分支、留用坐席 3 个、远端已合并分支）；④ progress 提交仍等用户；⑤ backlog：mobile-wave1-nits、B3-1、F-3/N-3、H-3、R-2 >50 条脚本场景。
- **review-c2**（13:36，pass）: 六条修复全部实测真修——雪藏树收藏 API `total:0`、61 条收藏 → `total=60 / 窗口 50 / 「还有 10 条」`、隐藏 chrome 锚点漂移 0.00px；合并完整、六条脚本全绿、桌面基线保持。遗留 minor/nit：C2-1 第 51 条起收藏 UI 不可达、C2-2 跨标签取消不同步、C2-3 到底时恢复补偿被 clamp → 入 backlog `mobile-wave3-nits`。`feat/mobile-wave3` @ f5ab232 可上线，等用户放行 `ship-c`（fj-ship-c-6211）。
- **ship-c**（21:5x–22:1x，`fj-ship-c-6211`）: 用户「上线吧」→ PR https://github.com/SmokingMouse/trellis/pull/41 → merge 4983ea33b → `make deploy REF=HEAD` → release `20260906T135924-4983ea33b`，gate up，lark 21:59 重连，迁移落地。冒烟一度被 prod 页面长连接卡死 agent-browser（裁决：只关自己 session、短超时重试），最终截图落盘。主控独立核实后放行。同轮用户提出：① 手机上开分链后 UI/交互与原链不一致（已起只读诊断单 `fj-branch-chain-diag-462d`，之后修复）；② 清理杂乱 tab（已关 3 个残留 pane、移除 7 个已合入 worktree/workspace）；③ 讨论 Trellis 打通 Herdr 作手机前端（主控给出评估：读侧已有 cli-sync，写侧改走 `herdr agent prompt`，blocked 用 send-keys；建议先起只读探查单，等用户拍方向）。
- **分链不一致（22:1x–23:20）**: 诊断单 `fj-branch-chain-diag-462d`（codex reuse，只读，隔离实例逐路径复现 a–f）给出四条根因：R1 anchor 同时承担引用语义与聚焦策略（选区追问留原链、卡片分支跳新链、编辑重问看节点来源）；R2 手机「新树」只关 picker 不关全屏树 sheet；R3 手机 linear 门控只看 sessionId，canvas 下从「最近」切同会话另一条链仍停 canvas（100% 复现）；R4 站内换链不更新 URL，重载回旧链（「偶发感」放大器）。主控清理 worktree 时误删了它的 workdir（wave3），结算改在 header-hide worktree 完成。修复 `feat/mobile-branch-chain`（reuse fixer，F1–F4 + `mobile-branch-chain.sh` 端口 3478）→ review-d fail（D-1：editNode 无门控改了桌面落点）→ **主控裁决认下统一**（decisions.md 记录）+ D-2 语义判定 / D-3 无 session 清 query / D-4 Cancel 取舍落档 → review-d2 **pass**（新增桌面 edit 落点断言、Cancel 断言、新会话首屏 URL 清空实测）。遗留 nit：D2-2 新会话首屏不可重载（入 backlog）。`ship-d` 契约已建（fj-ship-d-1089），等用户放行。
- **Herdr 打通讨论**: 用户否决 jsonl attach 路线，追问「以 Herdr 为基石、Trellis 方式渲染」。主控核实：herdr socket API 有订阅事件（pane.updated / pane.output_matched / agent_status_changed 等）与 ANSI 快照，无消息级 API；Claude Code hooks 给用户提示词/工具调用/Stop 但不给 assistant 正文（只给 transcript_path），codex notify 带 last-assistant-message。给出 H 方案（Herdr 不动，hooks 事件推送 + Stop 时读一次 transcript 末尾 + PreToolUse 审批回传做 Trellis 审批卡 + herdr agent prompt 输入）与 A 方案（Trellis 为运行时）。等用户对「Stop 时读一次 transcript 末尾」表态。
- **ship-d**（09-07 00:36）: 用户「上吧」→ #42 https://github.com/SmokingMouse/trellis/pull/42 合并 → release `20260906T163218-398d114ce` → gate up、lark 重连、prod iPhone 冒烟（slim header、同会话换链后 URL node 更新且仍 linear）通过；主控独立核实后放行。worktree w1W 已移除。
