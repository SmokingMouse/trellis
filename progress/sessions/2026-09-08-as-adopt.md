# 外部 agent-server 线程收编（交付待验收）

- 增加默认关闭的 TRELLIS_AS_ADOPT、启动分页扫描/退避、持久收编映射、删除墓碑与远端创建前的本地线程声明；复用现有节点、审批、中断和分叉路径。迟到的 Herdr 元数据会重新计算归属。
- 节点/会话携带 external 来源及 backend；空线程可经 API 首问，关闭后拒绝发送；删除运行中的收编会话仅释放本地观察连接，不中断或关闭远端线程。没有改 daemon/vendor、飞书、任务调度或旧 run-bus。
- 历史基线：AS project 全库 COUNT=2、fixture 会话 COUNT=1，源库已有其他 AS 线程导致全库断言稳定红。最新主控裁定确认应限定 fixture 会话范围；已恢复该断言并撤掉临时库绑定清理，保留完整生产快照。独占复跑 exit 0，证据 out/ruling-mobile-as-project.log 与 ruling-as-project-counts.txt。
- 空会话手机首问连续三次失败后曾按契约停机。主控现已裁定继续交付、仅收编至少一个 turn 的线程；删除空会话首问用例，替换成零 turn 不建会话和首轮完成后五秒内收编并准确回填。共用投影覆盖扫描快照和 turn 通知先于 items 到达的情况。hydrate 症状按主控裁定列为疑似通用空会话问题，本单不修。
- 继续交付后，新首 turn E2E exit 0；真机脚本 exit 0：自建 sonnet 临时线程先完成首轮、归外部会话、主页续问获得回复、删除后线程仍活，最后只关闭自己创建的线程，临时目录已清理。证据为契约 out/resumed-as-adopt-live.log、live-proof.json、live-close.log；没有给 fj 坐席发送输入、审批或关闭。
- 主控已确认系统 home/scratch 兜底根不参与祖先匹配，真实根多个命中取最长，无真实命中归外部会话。补充隔离单测覆盖主目录和 scratch 下的未登记目录，真实项目仍优先匹配；`bun test lib/server/as-adopt.test.ts` exit 0。真机脚本已有 trellis:external 断言，尚未执行。backend=external 的 turn/start 被协议明确拒绝这一裁定仍待答复。
- 前轮 AS project 的临时库绑定清理方案已按最新裁定撤回。最终版本采用 fixture 会话范围断言，本轮十一条手机脚本（新 adopt + 既有十条）统一前缀独占复跑全部 exit 0，日志 out/ruling-mobile-*.log 与 ruling-mobile-exits.txt。
- 最新裁定修订后 `bunx tsc --noEmit` exit 0、`bun test` exit 0（274 pass / 0 fail）；证据 out/ruling-check-exits.txt、ruling-bun-test.log。
- Next：主控独立验收及 backend=external 协议限制裁定；通用空会话 hydrate 留待专项。未 push、未部署。
