# 外部 agent-server 线程收编（partial）

- 增加默认关闭的 TRELLIS_AS_ADOPT、启动分页扫描/退避、持久收编映射、删除墓碑与远端创建前的本地线程声明；复用现有节点、审批、中断和分叉路径。迟到的 Herdr 元数据会重新计算归属。
- 节点/会话携带 external 来源及 backend；空线程可经 API 首问，关闭后拒绝发送；删除运行中的收编会话仅释放本地观察连接，不中断或关闭远端线程。没有改 daemon/vendor、飞书、任务调度或旧 run-bus。
- 隔离单项回归通过；最后一次全量 bun test 为 274 pass / 0 fail。既有 AS project 的早期 exit 0 使用了限定会话的断言，已按主控补充撤回该修改和 D4 通过结论；恢复 main 原脚本后独占复跑 exit 1，全库 COUNT=2、fixture 会话 COUNT=1，源库历史线程导致稳定基线红。其余 9 条既有手机脚本有 exit 0 记录。基础收编 E2E 曾完整 exit 0，包含回填、双向消息、审批、删除永久解绑、off 零请求和手机截图。
- 最终扩充 E2E 的空会话手机首问连续三次失败，按契约停止调试：后端空会话正确，浏览器 hydrate 请求中止。当前不能算 D3 通过，详见 failures.md 与契约 out/result.md。最后全量测试后仍有局部修订，尚未完成最终整套复跑。
- 常驻 daemon 仅做只读 thread/list 核对；没有创建真机临时线程，没有给 fj 坐席发送输入、审批或关闭。真机验收未完成。
- 两项语义已上报主控：系统 home/scratch 根的包含归属与指定 scratch→外部会话判据冲突；backend=external 的 turn/start 被协议明确拒绝。当前实现不改 daemon，拒绝结果如实返回。
- Next：主控裁定是否返工，定位空会话导航失败，再补真机证据与最终全量验证。未 push、未部署。
