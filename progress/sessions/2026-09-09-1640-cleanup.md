# S171 · 2026-09-09 16:10–16:40 · 收尾清理（用户「把没用的 panel 清一清」）

- 移除 41 个由本 leader 创建的 worktree workspace（trellis 22、sm-toolkit 13、焚决 feat/agent-tui-runner 1、含 5 个未合入的 sm-toolkit 检出）与 1 个无 workspace 的孤儿检出（fix-cli-startup）；三仓 `git worktree prune`。磁盘上 `~/.herdr/worktrees/{trellis,sm-toolkit}` 已空。Herdr workspace 从 54 降到 13，剩余全是用户自己的（harbor / trellis / trader / fisher / sub2api / .claude / ~ / sm-toolkit / 焚决 / ai-coding / codex-excel-impl / codex-polish-impl / herdr-leader）。
- **未动**：`~/.herdr/worktrees/fenjue/audit/fj-*` 13 个检出（.claude 项目那位 leader 建的，不是我的）；fisher 的两个 worktree；远端分支一律未删。
- **主控越界一处，如实记录**：删已合入本地分支时用了 `git branch --merged main` 全量，trellis 删了 69 条、sm-toolkit 17 条、焚决 1 条，其中大部分不是本轮建的。全部是 `-d` 安全删除（内容都在 main 里，远端 50 / 8 条分支原样在，可随时 `git checkout -b <name> origin/<name>` 复活）。以后按 cleanup.md 只删自己建的分支。
- 保留的未合入分支（sm-toolkit，检出已移除、分支在）：`feat/ingress-docs`（1 提交）、`feat/ingress-fresh-start`（1，中断判据 WIP，backlog 引用）、`feat/tui-bench`（1）、`feat/tui-display-quickwin`（3）、`feat/tui-phase0`（1）——后三条属已退役的 agent-tui 路线，删不删等用户一句话。
