# Backlog（需求侧：只收「用的时候撞到的摩擦」）

规则：一条 = 一次真实使用中的卡点，写「做 X 时，Y 卡住了」，带撞到次数；不收「应该支持 Z」。按疼痛排序，session 从顶上拉活。空清单是正常状态。状态：`open` / `doing` / `done(Sxxx)` / `wontfix(原因)`。

| # | 场景（做 X 时，Y 卡住了） | 撞到 | 状态 | 备注 |
|---|---|---|---|---|
| B1 | 想把 Agent 接到飞书机器人上：关联流程太长（开放平台侧多步手工配置），关联后私聊发消息机器人没反应 | 1（2026-09-02 用户口述） | done(S134) | 根因不在开放平台（小秘书三版本已发布、事件/权限齐全），在 Turbopack 内联真 ws 绕过 Bun 原生替换 → 握手失败且 SDK 不回调；PR #34 修，prod 私聊 6.7s 回包实测。原备注：向导只验凭证不验「消息真能进来」；Launcher 是裸链接无模板；prod `lark_bots` 0 行、日志零 `[lark` 痕迹；S125 Next①「真凭证端到端」从未闭环 |
| B2 | 在飞书里 @ 机器人之后，不知道这次交互「存在于哪」、和画布上正在跑的会话是什么关系 | 1（2026-09-02 用户口述） | doing | PR #36 已上线 prod（526d3ab），待群内四步验收后关闭。spec: im-entry-layer.md（四旋钮 + 话题即树 + @slug 外援）。现状：每个飞书 chat = 独立 `kind='lark'` 线性会话，与画布既有树无关；画布侧 @agent 是「换人设一轮」，两个 @ 语义不同；见 S134 提案 |
