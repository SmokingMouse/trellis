# agent-server 主页收编与 project 切流，详见 README 同名章节。
# off 是最高优先级硬关闸；on 启用默认 daemon；留空时配置 socket 也会启用。
TRELLIS_AS=off
TRELLIS_AS_PROJECT=off
# 至少有一个 turn 的外部线程自动收编（默认 off）；AS=off 覆盖。删除会话永久解绑，不 thread/close。
TRELLIS_AS_ADOPT=off
# 可选：只给此 projects.id 所属工作区的新会话打标；未知归属保持 legacy。
# TRELLIS_AS_PROJECT_ID=<projects.id>
# PROJECT=off 只停止新会话打标；AS=off 同时关闭收编和写入，project 新请求回退兼容模式，外部会话保留历史并拒绝发送。
# AS 未设时 SOCKET 可启用 AS；再设 PROJECT=on 即为新 project 会话打标。AS=off 始终覆盖 SOCKET。
# TRELLIS_AS_EFFORT=high
# 按实际 daemon 配置填写绝对路径；不设置时使用 agent-server 默认路径。
# TRELLIS_AS_SOCKET=/absolute/path/to/agent-server.sock
# TRELLIS_AS_TOKEN_PATH=/absolute/path/to/token
# 实时审批观察需 daemon 的 pendingRequests 能力；首次/重连 attach 快照后订阅通知，无 2 秒轮询。
# midThreadFork 能力自动协商：早期续聊/显式 fork 按节点 item 边界原生分叉，缺失时才播种祖先历史。
# 以上能力由 daemon initialize 声明，不需要另设环境开关；请同步升级独立运行的 daemon。
