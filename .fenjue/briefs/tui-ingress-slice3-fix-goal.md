目标：修 slice 3 复核的唯一 P1（复核报告 `/Users/smokingmouse/python/learning/trellis/.fenjue/tasks/fj-tui-ingress-slice3-review-52eb/out/review.md` 或归档同名目录，先读）：契约必测项「一个 TUI 连接里同时一 Codex 一 Claude 线程」没有被真正跑过，方案 §5 slice 3 的判据 `list_contains_both_backends` 被静默替换。你是 gpt-6-astra 实现坐席，分支 feat/codex-ingress-s3（本 worktree）。**先 `git merge feat/codex-ingress`**（含租约修复 129f581 与 slice 2 返工），解冲突后再动手。
要求：1) 冒烟脚本加判据 `list_contains_both_backends`：同一连接内起一 Codex 线程 + 一 Claude 线程（显式 sonnet），thread/list 与 loaded/list 同时含两者且 backend 元数据正确，两线程交替各发一轮（事件、审批、中断按线程隔离），picker 切换后视图正确；默认 `--expect` 加上它；两种后端各 3 次全过（本判据在两种 --backend 下都执行）。2) 若走查发现跨后端路由隐患一并修，单测覆盖。3) 全量测试与 typecheck 绿；协议文档更新。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`。提交到本分支，交付时 `git status --short` 为空。
