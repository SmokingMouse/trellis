复核 slice 3 二次返工 fj-tui-ingress-slice3-fix2-5f7f（产物 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-tui-ingress-slice3-fix2-5f7f/out/result.md`；再审 `.fenjue/archive/fj-tui-ingress-slice3-review2-f820/out/review.md` 的 P1-1 与 V7；裁决在 `progress/decisions.md`「ingress 对 TUI 级模型跨后端 override 的容忍」，同前缀）。你是 Opus 复核坐席（显示端是官方 Codex TUI），只读，产物 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/review.md`。分支 feat/codex-ingress-s3（本 worktree）。隔离 daemon，不碰生产。
必做：1) 官方 TUI 以 `--model sonnet` 启动（Claude 主会话）：新建 Claude 线程发一轮 → picker 切到 as/1 建的 Codex 线程 → resume 与发一轮成功、线程模型沿用 gpt-6-astra、TUI 侧收到可见提示；反向（`--model gpt-6-astra` 主会话切 Claude 线程）同样；同后端 override 仍生效；2) `wire_schema_clean`：自生成 0.153.4 schema 对全部 AS→TUI 帧逐条校验零不合规（含 requestUserInput 的 isBlocking）；3) 十二判据两后端各 3 次；4) 再审通过项抽核无回归。评级 P0/P1/P2；结论只能是「通过」或「需返工（列 P0/P1）」。不改源码；临时文件只放 mktemp。
