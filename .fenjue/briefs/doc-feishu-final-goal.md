用终稿重跑飞书转换并做导入预检，仍然不建文档。终稿：/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-doc-fix2-470f/out/article.md 与同目录 overview.svg（已两轮复审可交付）；你上一单的脚本与方案：/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-doc-feishu-prep-d29e/out/md2lark.ts、import-plan.md。做：(1) 复制 md2lark.ts 到本单 out/ 后用终稿跑，产出 out/feishu.xml（带校验外壳）与 out/payload.xml（剥壳后可直接传给 --content 的纯 payload）；xmllint 校验外壳版；核 SVG 清洗与 Mermaid 安全子集，无告警；统计顶层块数、表格数、画板数。(2) 预检：先 lark-cli auth status 确认可用身份（上次 user 身份缺失只有 bot），然后**只允许**带 --dry-run 的 lark-cli docs +create（--as 按可用身份，--content @out/payload.xml，--parent-position my_library）打印请求体到 out/dry-run.json，任何不带 --dry-run 的写命令一律禁止；比对请求体里标题、块数、画板块与 payload 一致。(3) out/preview.md：给主控出预览卡用的信息——标题、将使用的身份、顶层块数/表格数/画板数（类型）、正文前 120 字、建文档后需要的授权步骤（bot 身份则 member-add open_id ou_32b5b6977b4683a446c8069757286325 full_access）、以及一条完整可直接执行的建文档命令（不带 --dry-run 的最终版本，原样写在文件里但不要执行）与建后核对画板的命令。硬规则：不建文档、不改任何云端内容；不写凭证；payload 与 preview 中不得出现带用户名的绝对路径。完成发 result。blocker 期间不发 result。
