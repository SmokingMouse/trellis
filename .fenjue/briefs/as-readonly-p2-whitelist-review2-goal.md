五审只读命令免审名单（返工单 fj-as-readonly-p2-whitelist-fix-4742，产物 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-as-readonly-p2-whitelist-fix-4742/out/result.md`；四审 `.fenjue/archive/fj-as-readonly-p2-whitelist-review-d2fb/out/review.md` 的 P0-1 与 P2-1～P2-4，同前缀）。你是 Opus 复核坐席（显示端是官方 Codex TUI），只读，产物 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/review.md`。分支 feat/readonly-p2-whitelist（本 worktree）。隔离 daemon，不碰生产。
必做：1) 展开闸全局化反例矩阵：所有免审命令（含 git 各子命令、ls、cat、find、rg、grep、file 等）× 展开元字符（`{}` `*` `?` `[]` `~` `$var` `$(…)` 反引号 `<(…)` `>(…)`）× 位置（argv 任意位置、粘在选项上、`=` 值里）× 引号形态（无引号 / 单引号 / 双引号 / 转义），未引号一律 deny、引号内字面量按既有规则；真 shell 差分执行放行命令确认无写；2) 四审 P0-1 三条 git 向量真机（显式 sonnet，断言 init 帧 model）：产生审批且文件不落地；3) 历史 377 + 四审 194 + 67 差分固化的回归全过；4) P2-1～P2-4 处理核实（过拒面对照表合理、`-f` 路径根边界）；5) 全量测试与 typecheck。评级 P0（可绕过放行写）/ P1 / P2；结论只能是「通过」或「需返工（列 P0/P1）」。不改源码；临时文件只放 mktemp。
