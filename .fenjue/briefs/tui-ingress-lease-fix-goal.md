目标：修 codex-ingress 的租约语义（fj runner 实现单发现：`packages/agent-server/src/ingress/codex/router.ts:108` 对 full 权限的每次 resume 都 acquire 输入租约，默认 5 分钟、连接关闭才释放，导致 fj 这个独立 AS 客户端同期 `reply` / `thread/close` 撞 -32012 lease_held；证据 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-fj-runner-codex-tui-383b/out/{result.md,full-lease-probe.ts,full-lease-proof.log}`）。你是 gpt-6-astra 实现坐席，分支 feat/codex-ingress（本 worktree）。
leader 裁决（方案 §4.1）：resume / attach 不取租约；只在 `thread/permission/set` 升权时取短租约并在完成后立即释放；TUI 发普通输入沿用 AS 既有规则（仅当他人持有租约时拒绝）；`thread/close` 与 `turn/interrupt` 是生命周期操作，不受他人输入租约门控（核对 AS core 现状，若 close 被门控则一并放开并加测试）。
交付：1) router/session 改动 + 单测（resume 后 approvals/lease 表无租约行；升权取短租约并释放；他人持租约时 TUI 输入被拒的既有行为不变）；2) 冒烟脚本加判据 `external_client_reply_while_attached_ok`：官方 TUI 以 full resume 附着期间，另一 as/1 客户端能 turn/start（reply）与 thread/close；两种后端各 3 次全过；3) 全量测试与 typecheck 绿；文档更新。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`。提交到本分支，交付时 `git status --short` 为空。
