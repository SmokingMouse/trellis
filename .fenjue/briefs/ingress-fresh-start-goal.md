目标：修生产冒烟暴露的 P1——人类直接用官方 TUI 新建会话失败。你是 gpt-6-astra 实现坐席（显示端是官方 Codex TUI），分支 feat/ingress-fresh-start（基于 main 40ddc5b，已含 codex-ingress 全部），worktree 无 node_modules 先 `bun install`。
现象：对常驻 daemon（launchd，`~/.sm-toolkit/agent-server.sock.endpoint.json` 的 codexIngressUrl）执行 `codex --remote <url> --remote-auth-token-env FJ_CODEX_INGRESS_TOKEN`（不带 resume，让 TUI 自己新建线程）→ TUI 报「Error: Failed to start a fresh session through the app server: thread/start failed during TUI bootstrap: thread/start failed: as-ingress: native config ove…」（完整文案与 pty 见 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-smtk-ship-main-3d07/out/{prod-codex.pty,smoke-prod.py,smoke-prod.log}`）。fj 走「as/1 建线程 + TUI resume」的路径正常，说明是 TUI 自建线程时带的 native config override 被 ingress（slice 4 的副作用白名单或 slice 1 的 thread/start 映射）整体拒绝。
leader 裁决：TUI 冷启动 thread/start 里的 config override 按项分流——影响本线程自身的（model、reasoning effort、sandbox、approvalPolicy、cwd、personality 等）映射到 AS thread options（sandbox/approvalPolicy → AS permission，按既有映射表；model 过 model guard；serviceTier 非 default 拒绝）；纯展示/本地偏好项忽略并留 engineEvent 审计；真正改全局配置或磁盘的项（config write 类）才拒绝，且错误信息要指名哪一项。目标：用户本机默认 `~/.codex/config.toml` 下，`codex --remote <url>` 冷启动新建线程成功，Codex 与 Claude（在 picker 里选 sonnet）各能发一轮。
交付：1) 修复 + 单测（override 分流表）；2) 冒烟脚本新增判据 `fresh_tui_session_ok`（不 resume，TUI 自建线程，两种后端各 3 次），并把 `smoke-prod.py` 的逻辑收编为 `packages/agent-server/scripts/codex-remote-smoke.py --mode prod --endpoint <endpoint.json>`（对常驻 daemon 跑，不起隔离 daemon）；3) 全量测试、typecheck、升级检查脚本绿；4) `gh pr create --base main --head feat/ingress-fresh-start`（末尾「🤖 Generated with [Claude Code](https://claude.com/claude-code)」）→ `gh pr merge --merge --delete-branch=false` → `git -C ~/sm-toolkit pull --ff-only` → `(cd ~/sm-toolkit/packages/agent-server && bun run build)`；**不要重启生产 daemon**（launchd 重启由 leader 做），把重启后的验证命令写进 result.md。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`。
