目标：把焚决仓的 feat/agent-tui-runner（含 agent-tui runner、codex-tui runner、reporter、文档）合入 main。仓库无远端，本地合并即可：`git -C <本 worktree 的主仓> checkout main && git merge --no-ff feat/agent-tui-runner`（主仓路径用 `git worktree list` 找到；若 main 被别的 worktree 检出，在该 worktree 里合）。合并后在 main 上 `bun test` 全绿；`skill/scripts/fj.js` 的 sha 必须与安装副本 `/Users/smokingmouse/.claude/skills/herdr-leader/scripts/fj.js` 一致（若 main 上重新打包出的 sha 不同，说明源码与 bundle 不一致，报告并停下，不要动安装副本）。不删分支。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`（合并提交号、测试结果、sha 对照）。
