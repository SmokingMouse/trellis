目标：codex-ingress 文档收口（阶段：codex-tui runner 试点第 2 单，你是 Claude sonnet 坐席，显示端是官方 Codex TUI）。分支 feat/ingress-docs（基于 feat/codex-ingress），只改 docs 与 README。
交付：1) `docs/agent-server/protocol.md` 的 codex-ingress 章节按当前实现校对（slice 1–3 与租约修复：ws listener、control 进程、thread↔进程路由、Claude 线程单向合成与三条投影裁决、租约只在升权时取、开关 `codex_ingress.enabled` / `claude_threads` / `port`），删过期句，补「不支持方法表」与「升级回归：钉 codex-cli 0.153.4」；2) `docs/agent-server/README.md` 路线图：agent-tui 退役路径、codex-tui runner 使用方式（引用 herdr-leader 的 runners/codex-tui.md 要点）；3) 一页「人类使用指南」`docs/agent-server/codex-tui-quickstart.md`：如何用官方 `codex --remote ws://… --remote-auth-token-env …` 连本机 daemon、resume 某个线程、Claude 线程怎么选模型、审批卡长什么样、常见错误（未开 ingress / 错 token / 非 loopback）。
每个陈述对照源码（`packages/agent-server/src/ingress/codex/`）给出行号级依据写在 out/result.md；不改代码。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`。提交到本分支。
