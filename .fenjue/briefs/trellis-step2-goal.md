Trellis 迁移第二步「project 模式切流」，独立分支 feat/agent-server-step2（基于影子模式分支 feat/agent-server-client b691c8e，已有 as-client、vendor/agent-server、TRELLIS_AS 开关）。方案：/Users/smokingmouse/.herdr/worktrees/sm-toolkit/feat-agent-server/docs/agent-server/trellis-migration.md §3 第二步（先读全文与第一步的实现）；agent-server 已集成到 sm-toolkit feat/agent-server（/Users/smokingmouse/.herdr/worktrees/sm-toolkit/feat-agent-server，HEAD 7913839，protocol.md 是真源：engineEvent、engineControl、permission/set、effort、bash 输入、compact、lease 门禁）。交付：
(1) 重新 vendor：SM_TOOLKIT_DIR=/Users/smokingmouse/.herdr/worktrees/sm-toolkit/feat-agent-server sh scripts/vendor-agent-server.sh，VENDORED_FROM 记 7913839；as-client 升级到新协议（能力协商、engineEvent 订阅、permission/set、lease）。
(2) 会话绑定模型：sessions 增 binding_type（legacy | pane | thread，默认 legacy）与 as_threads 表（session_id、thread_id、daemon 标识、created_at）；lib/server/session-binding.ts 统一解析「这条会话此刻由谁驱动」；pane 绑定的实现在 feat/herdr-bridge 分支（herdr_sessions 表），本分支只留接口与文档说明，不实现 pane 分支。
(3) 切流：TRELLIS_AS=on 且 TRELLIS_AS_PROJECT=on 时，新建的 project 模式会话打 binding_type=thread；app/api/chat/route.ts 对 thread 绑定会话改走 as-client：thread/start（model / permission / effort 由会话与运行配置映射；权限模式按现有 mode 映射，plan 模式对应 plan）或复用已有 thread，turn/start 携带 prompt 与图片附件；AS 通知翻译成现有 RunEvent 喂现有 SSE 编码器（前端零改动为目标）；双写 DB（appendNodeResponse / appendToolCallStart / markToolCallDone / finalizeNode），turn/completed 时对账断言只 warn。存量会话与 chat/enhanced chat 照旧走 startRun；run-bus 不删。
(4) 分流：POST /api/nodes/[id]/respond 对 thread 绑定转发审批答复（serverRequest 解析，先取短租约）；abort 走 turn/interrupt；分叉走 thread/fork；@提及 ephemeral、自定义 agent 老路径回归不变。
(5) 网页与手机端：节点视图渲染权限模式（含 Shift+Tab 等价的切换控件，走 permission/set，需要租约；提权受门禁且只在建线程时能选 bypass）、审批卡（与 TUI 竞答：一端处理后另一端显示「已由 X 处理」）、可折叠系统日志带（engineEvent）；手机 44px 与不横向溢出。
(6) 降级：daemon 不可达时新请求回退 startRun 并显示一条提示；TRELLIS_AS_PROJECT 关闭即停止给新会话打标（回滚只需停打标）。
(7) 验收自动化：新写 scripts/mobile-verify/mobile-as-project.sh（沿用互斥锁、3471–3482、只用自己的 agent-browser 会话、env -i 可跑）：MockEngine 临时 daemon；开 AS 标记的 project 会话连发 3 轮、daemon 侧只有一个引擎（用 mock 计数）；网页 + 第二个 AS client 同时 attach 看同一份流，网页刷新 catchup 正确；审批一端处理另一端变已处理；中断、分叉、权限模式切换、系统日志带、降级回退各一条断言；DB 行与老路径同 prompt 的结构可比。单测覆盖翻译层与绑定解析。既有 mobile-as-shadow.sh、mobile-slim-shell.sh、mobile-new-session.sh、mobile-branch-chain.sh 回归不变。
约束：不 push；不动 sm-toolkit 分支与 main；不碰 3490 实例与 /tmp/trellis-mobile-verify.lock 之外的锁；只用 mock 引擎（真引擎复验由复核单做）；提交按 (1)–(7) 切小步。产物 out/report.md：做了什么、切流与回滚开关、绑定模型说明、怎么跑、开放问题（pane 绑定接线、多租户到 daemon 映射、发包）。blocker 期间不发 result。
