目标：sm-toolkit 上线（用户已授权，不再问）。你是 gpt-6-astra 实现坐席（显示端是官方 Codex TUI）。仓库：本 worktree 是 feat/codex-ingress（集成完成，最新提交见 git log），主仓 `~/sm-toolkit` 检出 main。
步骤：
1) `gh pr create --base main --head feat/codex-ingress`（标题「agent-server: codex-ingress + readonly gate + dogfood」，正文列四片与只读门收口、复核报告 cid、回退开关；末尾加「🤖 Generated with [Claude Code](https://claude.com/claude-code)」）→ `gh pr merge --merge --delete-branch=false`；合并后 `git -C ~/sm-toolkit pull --ff-only`，确认 main 含本分支（merge-base --is-ancestor）。
2) 在 `~/sm-toolkit` 执行 `bun install && (cd packages/agent-server && bun run build)`，dist 含 ingress（grep codex_ingress dist/daemon/runtime.js）。
3) launchd 常驻：用仓里模板 `scripts/agent-server/com.smokingmouse.agent-server.plist`（读其说明），生成 `~/Library/LaunchAgents/com.smokingmouse.agent-server.plist`，ProgramArguments 指向 `~/sm-toolkit/packages/agent-server/dist/daemon/cli.js run --grace-ms 5000`（bun 绝对路径），日志到 `~/.agent-server/launchd.{out,err}.log`，KeepAlive；配置文件 `~/.agent-server/config.toml` 已有 `[codex_ingress] enabled=true, claude_threads=true, port=0`，保留。
4) 切换：先 `sqlite3 ~/.agent-server/agent-server.db ".backup ~/.agent-server/backups/agent-server-<时间>-pre-launchd.db"`；确认 `fj status`（`/Users/smokingmouse/.claude/skills/herdr-leader/scripts/fj status`，FENJUE_ROOT=/Users/smokingmouse/python/learning/trellis）无在跑坐席（本单自己除外——你自己的线程会随重启断开，所以把重启放在最后一步并在 result.md 写清：切换命令、预期你的显示端断开、验证方法）；`kill -TERM <pane daemon pid>`（`pgrep -f "dist/daemon/cli.js run"`），等 10 秒确认退出且无自动重起（若有再 kill），`launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.smokingmouse.agent-server.plist`，`launchctl print gui/$(id -u)/com.smokingmouse.agent-server` 显示 running，`~/.sm-toolkit/agent-server.sock.endpoint.json` 含 codexIngressUrl 且 pid 为 launchd 起的进程。
5) 冒烟对常驻 daemon：不能用隔离 daemon——用 `codex-remote-smoke.py` 若支持 `--socket`/endpoint 指向生产 daemon 则两后端各 3 次，否则写一个最小脚本：官方 `codex --remote <codexIngressUrl>` PTY 起一线程、发一轮、`/quit`；两后端各一次。证据进 result.md。
6) 更新 `~/.claude/skills/herdr-leader` 的 policy？不要动；但把 `.fenjue/policy.md` 里 `agent_tui_bin` 与 `codex_bin` 保持不变（agent-tui 过渡期仍指 feat-tui-display-quickwin）。
产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`（PR 链接、合并提交、launchd 状态、endpoint、冒烟证据）。步骤 4 之后你的显示端会断，请把 result 与 `fj mail send` 在步骤 4 之前完成，并把步骤 4/5 写成可复跑脚本 `out/switch-to-launchd.sh` 与 `out/smoke-prod.sh`，由 leader 执行。
