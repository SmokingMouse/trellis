目标：修四审 P0-1（复核报告 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-as-readonly-p2-whitelist-review-d2fb/out/review.md` §P0-1 与 P2-1～P2-4，先读）。你是 gpt-6-astra 实现坐席（显示端是官方 Codex TUI），分支 feat/readonly-p2-whitelist（本 worktree）。
现象：新加的「glob / brace 展开会伪造 argv」闸只挂在 find / rg / grep / file 四个分支，`git` 分支没挂；真机 readonly 线程 `git log {--output=<绝对路径>,HEAD}` 免审放行且真写盘。
leader 裁决：展开闸必须是解析器级、对所有免审命令生效——任何未加引号的 shell 展开元字符（`{` `}` `*` `?` `[` `]` `~` `$` 反引号、`$(`、`<(`、`>(`）出现在任一 argv 即整条 deny（fail-closed），不按命令逐个补；引号内字面量按现有规则处理。
交付：1) 解析器级展开闸 + 单测（四审 P0-1 的三条 git 向量、find/rg/grep/file 的展开向量、以及「合法引号内 `*`」的放行对照）；三轮历史 377 例 + 四审自造 194 例 + 67 条真工具差分全部作为回归固化并全过；2) P2-1（rg 缺二进制恒审批）：名单自检 warn 之外，`model/list`/文档口径写明或从默认名单移除，二选一并说明；P2-2（展开闸字符集与 shell glob 选项无关，改为字符级保守判定）；P2-3（过拒面：给出常见只读写法的放行对照表，明显误拒的加白名单形式）；P2-4（`-f` 读任意文件作输入：readonly 语义下允许读，但路径须在 allowed_roots 内，否则审批）；3) 真机（显式 sonnet，断言 init 帧 model）：readonly 线程跑 `git log {--output=/tmp/x,HEAD}` 必须产生审批且文件不落地；4) 全量测试与 typecheck 绿；文档更新。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`。提交到本分支，交付时 `git status --short` 为空。
