对抗式复核「fj 坐席改走 agent-tui」的实现并做一次真实试点。实现交付说明 /Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-dogfood-impl-2bba/out/result.md（含主控切换步骤与回滚）；方案 /Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-as-dogfood-design-082e/out/design.md。两个仓：焚决 worktree 本目录（分支 feat/agent-tui-runner，git log 52ebf66..HEAD）与 sm-toolkit /Users/smokingmouse/.herdr/worktrees/sm-toolkit/feat-dogfood（分支 feat/dogfood，git log 7913839..HEAD）。
一、静态与 mock 复核：(1) fjContext 受限——服务端只接受白名单字段，注入任意 env / 路径穿越 / 超长值被拒；token 不出现在 argv、task.json、日志；(2) runner 优先级 CLI > 环境 > native，写进 task 后固定；(3) 首轮 RPC 注入幂等（重试不重建 thread、不重投契约）；(4) fj next 对 AS 单的停滞判定、催醒（steer）每心跳一次、退位顺序（close 引擎再关 pane）；(5) settle.ts / verify.ts 零改动；(6) launchd 模板：plist 字段、日志路径、KeepAlive/ThrottleInterval、卸载脚本；dry-run 即可，**不要安装**；(7) 文档 out/docs 与实现一致。复跑两仓 typecheck 与全部测试（焚决 bun test；sm-toolkit bun run typecheck + bun test）。
二、真实试点（唯一允许起真引擎的环节，≤3 个 codex turn）：用临时状态目录（如 /tmp/fj-pilot-$$，socket/token/db 都放里面，不用 ~/.sm-toolkit 默认路径，避免与将来生产撞车）在前台起临时 daemon（sm-toolkit worktree 的 dist/daemon/cli.js run，allowed_roots 含临时 scratch 仓与 FENJUE_ROOT）；建一个临时 git scratch 仓并用**焚决仓内的新 bundle**（bun /Users/smokingmouse/.herdr/worktrees/焚决/feat-agent-tui-runner/skill/scripts/fj.js …，不要用 ~/.claude/skills 里主控正在用的那份）以独立 FENJUE_ROOT 做 fj init、写一份玩具契约（只读：让坐席读 scratch 仓 README 并把一句事实写到 out/hello.md）；fj task launch --runner agent-tui --kind codex 显式 -m gpt-6-astra 且 service_tier default，锚点用本 workspace 的根 pane（w2B:p1）；观察：契约是否经 RPC 注入（pane 里不应出现 agent prompt 的键盘粘贴）、Herdr agent_status 是否随 thread 忙闲变化、task.json 记录 thread id、坐席 fj mail 能写回独立信箱、settle 复跑 verify 通过、关单退位后 thread/close 与 pane 关闭顺序、daemon 日志无异常；再演练一次 daemon SIGKILL 后 fj next 的表现与显式恢复。全部结束：关闭试点坐席 pane（只关自己起的）、停 daemon、删临时目录，确认无残留 codex/bun 进程与 socket。
三、只读：不改两仓文件；不碰 ~/.claude 下任何文件（尤其 fj.js，验收会字节比对）；不安装 launchd；不 push。产物 out/review.md 首行「结论：可切换」或「结论：需返工」，问题按 P0/P1/P2 编号（file:line、复现、修法），附试点全程命令与关键输出（thread id、mail 序列、settle 结果、清理证据）。blocker 期间不发 result。
