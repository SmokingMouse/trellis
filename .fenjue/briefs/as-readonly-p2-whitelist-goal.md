目标：只读命令免审名单的 P2-5（三审/四审遗留）：`find` / `rg` / `grep` / `file` 的参数校验仍是「枚举写标志再拒绝」的黑名单结构，改成与 `git branch` 同款的选项白名单（只允许明确列出的只读 flag 与形式；未知选项、`-exec`/`-delete`/`--pre`/`-C -m` 等一律 deny），并把三轮渗透测试的全部历史向量（一审 `fj-as-readonly-allow-review-*`、二审 `-review2-5f0a`、三审 `-review3-8b89`，路径前缀 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/`）作为回归用例保留全过。分支 feat/readonly-p2-whitelist（基于 feat/codex-ingress），worktree 无 node_modules 先 `bun install`。文件：`packages/agent-server/src/engines/readonly-commands.ts` 与其测试。约束：fail-closed 性质不变；`packages/agent-server` 全量测试与 typecheck 绿；文档「已知限制」相应更新。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`（列每个命令的白名单表）。提交到本分支。
