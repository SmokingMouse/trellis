复核 codex-ingress slice 3 返工 fj-tui-ingress-slice3-fix-65a8（产物 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-tui-ingress-slice3-fix-65a8/out/result.md`，先读，尤其它自述在「Claude 主会话」下发现的 TUI `--model sonnet` 后 resume Codex 线程的问题与处理；上一轮复核 `.fenjue/archive/fj-tui-ingress-slice3-review-52eb/out/review.md`）。你是 Opus 复核坐席（显示端是官方 Codex TUI），只读，产物 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/review.md`。分支 feat/codex-ingress-s3（本 worktree，已合 feat/codex-ingress 含租约修复）。隔离 daemon，不碰生产。
必做：1) `list_contains_both_backends` 真机原样重打：同一官方 TUI 连接内一 Codex 一 Claude（显式 sonnet）交替各发一轮、审批与中断按线程隔离、thread/list 与 loaded/list 同时含两者且 backend 元数据正确、picker 切换视图正确；分别以 Codex 主会话与 Claude 主会话（TUI 以 `--model sonnet` 启动）各做一次；2) 十判据冒烟两后端各 3 次；3) 上一轮复核通过项抽核（fork 边界、分页 schema、断线重放）不回归；4) 合并后（含 129f581 租约修复）`external_client_reply_while_attached_ok` 仍成立。评级 P0/P1/P2；结论只能是「通过」或「需返工（列 P0/P1）」。不改源码；临时文件只放 mktemp。
