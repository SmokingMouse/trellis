目标：Trellis 四波集成（分支 feat/ship-d，基于 main 21de979；本 worktree 无 node_modules 先 `bun install --cwd .`）。按顺序合并并解冲突：feat/herdr-bridge（19d0438）→ feat/agent-server-client（影子模式，b691c8e）→ feat/agent-server-step2（22cc2f0，依赖影子）→ feat/mobile-nits-wave3（f7dba61）→ feat/mobile-nits-wave1。每合一步跑 `bunx tsc --noEmit` 与 `bun test`。全部合完：七条手机脚本 `scripts/mobile-verify/*.sh` 整套独占跑绿（safe-area 那条在高负载下已知脆弱，失败就记录一次输出不重试）；AS 影子模式与第二步的 E2E 与手机验收（见 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-trellis-step2c-31e1/out/` 与 `fj-trellis-step2c-review-65a0/out/` 的脚本与命令，同前缀）在隔离实例上跑：隔离实例起法——`sqlite3 <生产库> ".backup <mktemp>/db"`、`TRELLIS_DB_PATH=<副本> HOME=<mktemp home>`、`next build` 后 `next start`（dev 会炸）、`TRELLIS_LARK=off`、本地 curl 要 `--noproxy '*'`；生产 daemon socket `~/.sm-toolkit/agent-server.sock` 可用（已开 ingress），AS 相关 E2E 用它或隔离 daemon 均可。不重新 vendor agent-server（下一单做）、不部署、不 push。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`（合并顺序与冲突解法、各脚本输出摘要）。提交到本分支。
