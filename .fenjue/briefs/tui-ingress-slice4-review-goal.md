复核 codex-ingress slice 4「治理硬化与升级回归」（fj-tui-ingress-slice4-8704，产物 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-tui-ingress-slice4-8704/out/result.md`，先读；方案 `.fenjue/archive/fj-tui-ingress-design-cd8e/out/tui-ingress-design.md` §1.2/§4.3/§5 slice 4/§7，同前缀；backlog 两条 daemon 缺陷见 `.fenjue/backlog.md` 的 agent-server-busy-after-engine-death 与 agent-tui-detach-interrupts-turn）。你是 Opus 复核坐席（显示端是官方 Codex TUI），只读，产物 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/review.md`。分支 feat/codex-ingress-s4（本 worktree，已合 s3@97d3391）。隔离 daemon，不碰生产。
必做（全部真机）：1) `codex --remote unix://…` 连通并完成六判据 3/3；endpoint.json 暴露路径；2) 副作用方法白名单 fail-closed：对官方 0.153.4 客户端方法表逐条打（config write、fs/write、fs/remove、command/exec、thread/shellCommand、plugin/marketplace、workspace 等 + 一个不存在的方法）：full 线程按策略放行/拒绝、readonly 线程显式 deny 且有审计行、未知方法拒绝且留痕；3) 两条 daemon 缺陷复现→已修：kill 引擎进程后 thread/close 成功不报 Thread is busy；turn 进行中关掉显示端（agent-tui 与 codex TUI 各一次）turn 继续完成；4) 升级回归脚本 `codex-ingress-upgrade-check.sh` 在当前 0.153.4 下可跑且输出差异报告为空；schema 生成物入库；5) slice 2 二审 P2×3 处理核实；6) 两后端全部判据冒烟各 3 次、全量测试与 typecheck、与 s3 合并后零回归。评级 P0（无审批写 / 未知方法放行 / 线程丢）/ P1 / P2；结论只能是「通过」或「需返工（列 P0/P1）」。不改源码；临时文件只放 mktemp。
