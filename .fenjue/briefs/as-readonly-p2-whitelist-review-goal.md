复核只读命令名单 P2-5 白名单化（fj-as-readonly-p2-whitelist-5c83，产物 `/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-as-readonly-p2-whitelist-5c83/out/result.md`）。你是 Opus 复核坐席（显示端是官方 Codex TUI），只读，产物 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/review.md`。分支 feat/readonly-p2-whitelist（本 worktree）。
必做：1) 三轮渗透测试的全部历史向量（一审 `fj-as-readonly-allow-review-*`、二审 `-review2-5f0a`、三审 `-review3-8b89`，同前缀 archive）在新解析器上逐条重打，0 UNSAFE-ALLOW；2) 针对 find / rg / grep / file 的新白名单自造反例：选项合并（`-rl`）、`=` 形式、重复选项、`--` 之后、长选项前缀匹配、`-exec`/`-execdir`/`-ok`/`-delete`/`-fprint*`、rg `--pre`/`--pre-glob`/`--hostname-bin`、grep `--include` 与 `-f`（读文件作 pattern）、file `-C`/`-m`/`-f`；每条给判定与证据；3) 白名单是否 fail-closed（未知 flag → deny）；4) 真机一次：readonly 线程（显式 sonnet，断言 init 帧 model）跑名单内 `rg` 或 `find` 免审留痕、跑 `find -delete` 产生审批；5) 全量测试与 typecheck。评级 P0（可绕过放行写）/ P1 / P2；结论只能是「通过」或「需返工（列 P0/P1）」。不改源码；临时文件只放 mktemp；不碰生产 daemon（用隔离 daemon）。
