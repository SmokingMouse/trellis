目标：焚决 fj 新增 `--runner codex-tui`：fj 坐席的显示端改为官方 Codex TUI（`codex --remote`）接 agent-server 的 codex-ingress；agent-tui 退役路径。你是 gpt-6-astra 实现坐席，仓库 = 本 worktree（焚决 feat/agent-tui-runner 分支，fj 源码在 src/，bundle 在 skill/scripts/fj.js；现有 agent-tui runner 的实现是你的起点）。
方案（必读）：`/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-tui-ingress-design-cd8e/out/tui-ingress-design.md` §1.3 一个 TUI 连接 = 一个 AS client、§4.5 fjContext、§6 agent-tui 退役路径（ready-file 协议删除、由 fj 自己建线程、Herdr 状态旁挂 reporter ~120 行）。ingress 协议事实：`/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-tui-ingress-slice1-9065/out/result.md` 与 `.fenjue/archive/fj-codex-native-tui-spike-23ce/out/codex-native-tui-spike.md`（同前缀）；ingress 代码在 `/Users/smokingmouse/.herdr/worktrees/sm-toolkit/feat-codex-ingress/packages/agent-server/src/ingress/codex/`（只读参考，那边有复核在跑，不要改）。
交付：
1. `fj task launch <cid> --runner codex-tui --kind claude|codex --model … --permission … [--service-tier default]`：fj 经 as/1 自己建线程（fjContext、model、permission、cwd、serviceTier）并注入契约（turn/start，与今天一致），然后在 pane 里起 `codex --remote <ingress url> resume <native uuid>`（bearer 走 `--remote-auth-token-env <ENV>`），不再有 ready-file 握手；close = as/1 thread/close + 关 pane；`fj next` 的恢复命令改成 codex --remote 形式。
2. Herdr 状态：旁挂 reporter（订阅 as/1 通知 → herdr agent 状态 working/blocked/waiting/done），独立小进程或 fj 内部，≤ 150 行；显示端断开不影响线程（detach ≠ close）。
3. 配置：policy frontmatter 新增 `codex_bin`、`codex_ingress_url`（默认 ws://127.0.0.1:<port>）、`codex_ingress_token_env`；`FJ_RUNNER_DEFAULT=codex-tui` 可切默认；文档 `skill/runners/codex-tui.md`（或 references/runners 对应位置）+ SKILL/playbook 的调用面更新。
4. 验证：`bun test` 全绿；`scripts/smoke-codex-tui-runner.sh`：在 mktemp 里 clone sm-toolkit feat/codex-ingress、`bun install && bun run build`、起隔离 daemon（`codex_ingress.enabled=true`、`claude_threads=true`、随机端口、独立 HOME/DB），用 fj 对一个假契约走 launch → 官方 TUI 出现在 PTY 且线程 resume 成功 → 注入 → close，exit 0；Claude 与 Codex 各一次（Claude 显式 sonnet，只需 1 turn）。重新打包 `skill/scripts/fj.js`。
不改 sm-toolkit；不动安装副本 `~/.claude/skills/herdr-leader`（切换由 leader 做）。产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`。提交到本分支。
