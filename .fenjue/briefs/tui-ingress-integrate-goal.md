目标：codex-ingress 集成（也是 codex-tui runner 常规单）。你是 gpt-6-astra 实现坐席，分支 feat/codex-ingress（本 worktree）。先读三份最新复核确认无未收口 P0/P1：`/Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-tui-ingress-slice3-review3-93dc/out/review.md`、`fj-tui-ingress-slice4-review-b917`、`fj-as-readonly-p2-whitelist-review2-9f49`（同前缀）；若某份结论是「需返工」，先在对应分支修其 P0/P1（单测 + 冒烟）再合。
步骤：1) 依次 `git merge --no-ff feat/codex-ingress-s3`、`feat/codex-ingress-s4`、`feat/readonly-p2-whitelist`，解冲突（s4 已含 s3 早期提交，以 s3 最新为准）；2) `bun install`，`packages/agent-server` 全量测试与 typecheck 绿；3) 冒烟：`codex-remote-smoke.py` 两后端全部判据各 3 次（ws），Codex `--transport unix` 3 次；`codex-ingress-upgrade-check.sh` 差异为空；4) 文档合并去重（protocol.md codex-ingress 章、codex-tui-quickstart、README 路线图、只读名单已知限制）；5) 产出 `$FENJUE_ROOT/.fenjue/tasks/$FENJUE_CID/out/result.md`（合并提交号、冲突清单、判据表）。提交到本分支，交付时 `git status --short` 为空。不 push、不合 main（下一单做）。
