实现「fj 坐席改走 agent-tui」，严格按方案 /Users/smokingmouse/python/learning/trellis/.fenjue/archive/fj-as-dogfood-design-082e/out/design.md（§2–§8）做，本单覆盖方案 §8 的 A、B、C1、C2、C3、D、F 组，E 组（Trellis）不做。两个 worktree：fj 源码在本 worktree（焚决仓，分支 feat/agent-tui-runner，src/cli/main.ts、src/core/{launch,task,next,seat,status,herdr}.ts 等）；agent-server 与 agent-tui 在 /Users/smokingmouse/.herdr/worktrees/sm-toolkit/feat-dogfood（sm-toolkit 分支 feat/dogfood，基于已集成的 feat/agent-server 7913839），两边各自小步提交。交付：
(A) 协议：thread/start 增加受限的 fjContext（FENJUE_CID、FENJUE_ROOT、可选 seat 名）与显式 model / permission / serviceTier，服务端校验、持久化、映射到子进程环境；不得开放任意 env；协议文档与 schema 同步；fake 测试覆盖非法上下文拒绝、双 thread 不同 FENJUE_ROOT/CID 隔离。
(B) agent-tui：--backend/--model/--permission/--service-tier 参数；稳定 clientThreadId（幂等重试不重建 thread）；ready-file（就绪后写文件供 fj 等待）；--await-first-turn；把 thread id 与状态上报给 Herdr（pane.report_agent_session 或等价），让 herdr agent_status 仍能判忙闲；恢复提示。
(C1/C2) fj：`fj task launch --runner native|agent-tui`（缺省 native；FJ_RUNNER_DEFAULT 环境变量作新任务默认；runner 写进 task.json 后固定）；agent-tui 路径的起位序列：切 pane → 起 agent-tui（显式 model，缺 model 或 fable 一律拒绝；codex 走 gpt-6-astra + service_tier default）→ 等 ready-file → 经 AS RPC 完成 thread/start（带 fjContext）→ 契约作为首轮 turn 经 RPC 注入（不走键盘）→ thread id 记入 task.json；`fj next` 对 AS 单读 thread 状态判停滞、催醒经 RPC；`fj reply`/`rework` 的注入走 RPC；settle.ts / verify.ts 不改；退位与复用：thread/close 与 lease 语义；回滚：runner 恢复 native 的操作路径与文档。
(C3) 构建：在焚决仓把 bundle 产到 skill/scripts/fj.js（或仓内既有构建目标），**不要安装到 ~/.claude/skills/herdr-leader/scripts/fj.js**（那是主控正在用的实弹，切换由主控做）；bundle 不得打进 sm-toolkit 工作树的绝对 import。
(D) sm-toolkit scripts/agent-server/ 下 launchd plist 模板与安装/卸载脚本 + README（用户级 LaunchAgent、socket/token 路径、日志、崩溃重启），**不要实际安装或启动 launchd 服务**；测试用临时 daemon。
(F) 文档：runners/agent-tui.md 与 playbook/seats 的增补——写到焚决仓的 skill/ 下对应位置（若 skill/ 是 herdr-leader 的源，就改源；不是就放 out/docs/），说明 native/AS 区别、模型显式、普通档、权限、恢复、退出标准。
阶段 0 预验收（方案 §7）：用 MockEngine/fake-codex + 假 Herdr 跑：丢 ready、丢 RPC 响应、进程断开三种注入点重试后恰好一个 thread、一个首轮 turn；缺 model 与 fable 被拒；full 映射正确；两个 root/cid 的 mail 路由隔离；原 native 路径回归不变。
约束：不 push；不动 main、feat/agent-server；不安装 launchd、不改 ~/.claude 下任何文件、不起真 claude/codex（真实试点由主控另起）。两边 typecheck 与全部测试绿。产物 out/result.md：按 A–F 列改动文件、测试名、切换步骤（主控如何安装 bundle 与起 daemon 试点）、回滚步骤。blocker 期间不发 result。
