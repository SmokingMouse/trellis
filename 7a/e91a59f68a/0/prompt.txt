<local-command-stdout>Goal set: 现在卡片图好像没法复制，正文也没法直接复制，基于 master 代码开发修复，然后提交 PR</local-command-stdout>

A session-scoped Stop hook is now active with condition: "现在卡片图好像没法复制，正文也没法直接复制，基于 master 代码开发修复，然后提交 PR". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.